"""Sorting implementation — this is the file the agent optimizes."""


def sort_array(arr: list[int]) -> list[int]:
    """Sort arr in-place and return it. Baseline: Python built-in sort."""
    arr.sort()
    return arr
